<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>login page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

<div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <div class="w-full flex justify-end">
        <a class="bg-red-500 px-4 py-1 rounded-md border-none" href="/logout">logout</a>
    </div>

    <h1 class="text-3xl mb-2">Hello , <%= user.name %></h1>
    <h5>you can create your post here..</h5>

    <form action="/post" method="post">
        <textarea 
        class="resize-none bg-transparent border-2 border-white/20 rounded-md mt-3 p-3 w-1/3 block"
        placeholder="what's on your mind.." 
        name="content"></textarea>
    
       

        <input 
            class="bg-blue-500 px-5 py-2 rounded-md border-none block mt-3" 
            type="submit" 
            value="post">
    </form>

    <div class="posts mt-4">
        <h5 class="text-2xl text-zinc-500 mb-2">your posts..</h5>

        <% user.posts.reverse().forEach(function(post){ %>
            <div class="postcontainer mb-2">
                <div class="w-1/3 p-4 rounded-md border-2 border-white-200 bg-transparent">
                    <h2 class="text-blue-500"><%= user.name %></h2>
                    <p class="mb-3  break-all"><%= post.content %></p>
                    <small><%= post.likes.length %> likes</small>
                    <div class="btns flex gap-4 mt-1">
                        <a class="text-blue-500" href="/like/<%= post._id%>"  > <%= post.likes.indexOf(user._id)? "Like" : "Unlike" %></a>
                        <a class="text-orange-500" href="/edit/<%= post._id%>">edit</a>
                    </div>
                    
                </div>
            </div>
        <% }) %>

    </div>
</div>

</body>
</html>
