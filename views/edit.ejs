<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">

<div class="w-full min-h-screen p-5">

    <div class="w-full flex justify-end">
        <a class="bg-red-500 px-4 py-1 rounded-md" href="/logout">Logout</a>
    </div>

    <h5>You can edit your post here..</h5>

    <form action="/update/<%= post._id%>" method="post">
        <textarea
            class="resize-none bg-transparent border-2 border-white/20 rounded-md mt-3 p-3 w-1/3 block"
            placeholder="What's on your mind..."
            name="content"><%= post.content %></textarea>

        <input
            class="bg-yellow-500 text-black px-5 py-2 rounded-md block mt-3 cursor-pointer"
            type="submit"
            value="Update">
    </form>

</div>

</body>
</html>
